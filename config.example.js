/* ==========================================================================
   CONFIGURACIÓN DEL SISTEMA Y CLAVES (PLANTILLA EJEMPLO)
   Instrucciones:
   1. Copia este archivo y renómbralo como 'config.js' en la raíz.
   2. Ingresa tus llaves reales en 'config.js'.
   3. 'config.js' está protegido por .gitignore y NUNCA se subirá a GitHub.
   ========================================================================== */

window.CONFIG = {
    // Credenciales de EmailJS (https://www.emailjs.com/)
    EMAILJS_PUBLIC_KEY: 'YOUR_PUBLIC_KEY',
    EMAILJS_SERVICE_ID: 'YOUR_SERVICE_ID',
    EMAILJS_TEMPLATE_ID: 'YOUR_TEMPLATE_ID'
};
